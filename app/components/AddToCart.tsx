"use client";

const AddToCart = () => {
  return (
    <>
      <button
        onClick={() => console.log("click")}
        className="btn btn-primary"
      >
        Add to Cart
      </button>
    </>
  );
};

export default AddToCart;
